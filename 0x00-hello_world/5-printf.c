#include <stdio.h>
/**
 * main - Entry point
 * printf - Print things with so many options of printing
 * Return: Always 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
